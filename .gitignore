node_modules/
dist/
coverage/

.env
.env.*
!.env.example

# OAuth refresh token — grants ongoing read access to the whole mailbox
.tokens.json

# Personal owner profile: card tails and name (see owner.example.json)
owner.json

# Ingested mail. The corpus never leaves this machine.
data/

# Real mail fixtures contain PII — local only. Ship synthetic samples instead.
eval/fixtures/*
!eval/fixtures/.gitkeep
!eval/fixtures/example.sample.json

.DS_Store
*.log
eval/_scratch/
# Rendered briefs contain real balances, due dates and counterparties.
eval/_out/

# Recorded model responses: keyed by a hash of the redacted prompt, but the
# prompt still contains his mail. Local only.
eval/cassettes/
